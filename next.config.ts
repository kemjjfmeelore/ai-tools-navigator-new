import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack is not enabled by default for next build in Next.js 14,
  // so no specific "turbo" flag is needed here to disable it.
  // This file remains empty as per default.
};

export default nextConfig;